<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>视差滚动</title>
    <style>
        html,body{
            width: 100%;
            height: 100%;
            padding: 0;
            margin: 0;
        }
        .bg-box{
            width: 100%;
            height: 100%;
            overflow-y: auto;
            overflow-x: hidden;
            perspective: 1px;
            position: relative;
            background-color: #12daff;
        }
        .bg{
            position: absolute;
            top: 0;
            left: 50%;
            width: 500%;
            height: 3000%;
            transform: translateX(-50%);
            background: url('/images/clouds2.png') repeat;
        }
        .bg1{
            transform: translateZ(-1px) scale(2.2);
            animation: move 40s alternate infinite;
         }
        .bg2{
            background-position: 880px -280px;
            transform: translateZ(-5px) scale(6.2);
            animation: move 35s alternate-reverse infinite;
         }
        .bg3{
            background-position: -400px 480px;
            transform: translateZ(-8px) scale(9.2);
            animation: move 50s alternate infinite;
         }
        @keyframes move {
            from{
                margin-left: -50%
            }
            to{
                margin-left: 50%;
            }
        }
    </style>
</head>
<body>

    <div class="bg-box">
        <div class="bg bg1"></div>
        <div class="bg bg2"></div>
        <div class="bg bg3"></div>
    </div>

</body>
</html>
